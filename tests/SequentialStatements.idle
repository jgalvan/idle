type Main class {
    main() void
    var a int;
    var b int;
    var c int;
    var d int;
    var e float;
    var f bool;
    var g string;
    {
        a = b + c;
        a = (a+b)*d+a*a+c;

        e = b*e;
        e = (a+b)/c*d+a/b+c;

        f = a > e;

        IO.print(a);
        IO.print(e);
        IO.print(f);

        a = IO.readInt();
        e = IO.readFloat();
        g = IO.readString();
    }
}