type Animal class {
    name string;
    age int;

    GetName() string {
        return $name;
    }

    SetName(name string) void {
        $name = name;
    }
}

type Dog <- Animal class {
    breed string;

    bark(woof string) bool {
        IO.print(GetName());
        if $age > 1 {
            IO.print("I'm older than 1");
            return true;
        }
        IO.print("I'm 1 or younger");
        return false;
    }

    setAge(age int) void {
        $age = age;
    }

    Dog(name string, breed string)
    var mydog Dog;
    {
        IO.print("Corri");
        mydog.SetName(name);
        IO.print(mydog.GetName());
        return mydog;
    }
}

type Main class {

    main() void
    var name string;
    var mydog Dog;
    var sum int;
    var i int;
    {
        IO.print("Enter name: ");
        name = "Firulais";
        //mydog = new Dog(name, "Chihuahua");
        mydog.SetName(name);
        IO.print(mydog.GetName());
        mydog.bark("puta madre");
        mydog.setAge(4);
        mydog.bark("puta otra vez");


/*
        sum = 0;
        for i = 0; i < 10; i = i + 1 {
            sum = sum + i;
        }
        //IO.print(sum);

        while true && false {
            // do stuff
        }

        if false {
            // do stuff
        }
        else if true {
            // do stuff
        }
        else {
            // do stuff
        }*/
    }
}
