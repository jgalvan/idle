type Main class {
    main() void
    var a int;
    var b int;
    var c int;
    var d int;
    var i int;
    {
        while a > a*b {
            a = a + b;
            while(a < b) {
                b = b * b;
            }
        }

        for i = a; i < a; i = i+a {
            b = a *b;
            IO.print(a);
        }

        if a >= b {
            a = b;
            if a == b {
                IO.print(a);
            } else {
                IO.print(b);
            }
        } else if a > c {
            a = c;
        }
    }
}