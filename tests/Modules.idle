type Main class {
    name string;
    age int;

    isAdult() bool
    {
        if $age >= 18 {
            return true;
        }
        return false;
    }
    
    main() void
    var ret_val string;
    {

        IO.print("Enter name: ");
        $name = IO.readString();
        IO.print("Enter age: ");
        $age = IO.readInt();
        IO.print("Hello my name is ");
        IO.print($name);
        if isAdult() {
            IO.print("Si soy adulto.");
        } else {
            IO.print("No soy adulto.");
        }
        
    }
}