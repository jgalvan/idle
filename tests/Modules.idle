type Main class {
    name string;
    age int;

    isAdult() bool
    {
        var myvar int;
        myvar = 1;
        if $age >= 18 {
            var myvar int;
            return true;
        }
        return false;
    }
    
    main() void
    {
        var ret_val string;
        ret_val = "test vars";
        IO.print(ret_val);
        IO.print("Enter name: ");
        $name = IO.readString();
        IO.print("Enter age: ");
        $age = IO.readInt();
        IO.print("Hello my name is ");
        IO.print($name);
        if isAdult() {
            IO.print("Si soy adulto.");
        } else {
            IO.print("No soy adulto.");
        }
        
    }
}