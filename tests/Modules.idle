type Main class {
    name string;
    age int;

    fibonacci(x int) int {
        IO.print(x);
        if x == 0 {
            return 0;
        }
        else if x == 1 {
            return 1;
        }
        return fibonacci(x-1) + fibonacci(x-2);
    }

    isAdult() bool
    {
        var myvar int;
        myvar = 1;
        if $age >= 18 {
            var myvar int;
            return true;
        }
        return false;
    }
    
    main() void
    {
        var ret_val string;
        ret_val = "test vars";
        IO.print(ret_val);
        IO.print("Enter name: ");
        $name = IO.readString();
        IO.print("Enter age: ");
        $age = IO.readInt();
        IO.print("Hello my name is ");
        IO.print($name);
        if isAdult() {
            IO.print("Si soy adulto.");
        } else {
            IO.print("No soy adulto.");
        }
        
        IO.print(fibonacci(6));
    }
}