type Main class {
    printFunc(a string) void
    {
        IO.print(a);
    }

    returnFunc(a string) string
    var b int;
    var c int;
    {
        if b > c {
            return a;
        }
    }
    
    main() void
    var ret_val string;
    {
        ret_val = returnFunc(ret_val);
        printFunc(returnFunc(ret_val));
    }
}