type Main class {
    name string;
    age int;

    printFunc(a string) void
    var c float;
    var b float;
    {
        IO.print(a);
        b = 1*b;
        b = 1*b;
    }

    returnFunc(a string) string
    var b int;
    var c float;
    {
        if b > c + 1 {
            return a;
        }
        return a;
    }
    
    main() void
    var ret_val string;
    {
        ret_val = "hello";
        $name = returnFunc(ret_val);
        printFunc(returnFunc(ret_val));
    }
}