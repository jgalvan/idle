type Main class {
    voidRecursion(n int) void {
        print(n);

        if n == 0 {
            return;
        }

        voidRecursion(n-1);
    }

    main() void {
        voidRecursion(10);
    }
}